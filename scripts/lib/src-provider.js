// Generated by CoffeeScript 2.5.1
(function() {
  /*
  	Source provider
  	Autor: Tsvikevich Denis 2020
  */
  var button, provider, showSources;

  //: import flexibel.coffee
  button = div("Показать источники");

  provider = element("src-provider");

  provider.after(button);

  showSources = function() {
    var element, i, j, k, l, len, len1, ref, ref1, results;
    k = 0;
    ref = provider.children;
    for (j = 0, len = ref.length; j < len; j++) {
      i = ref[j];
      i.innerHTML = "[" + (k + 1) + "] " + i.innerHTML;
      k += 1;
    }
    provider.prepend(h5("Источники"));
    provider.style.display = "block";
    button.style.display = "none";
    ref1 = element(".src-provider-info");
    results = [];
    for (l = 0, len1 = ref1.length; l < len1; l++) {
      element = ref1[l];
      results.push(element.style.display = "initial");
    }
    return results;
  };

  button.id = "src-provider-button";

  button.onclick = showSources;

}).call(this);
